#!/bin/bash

# Script to make all Modern section data props optional with default values

echo "🔧 Making all section components use optional data props..."

# This script adds default data to each section component
# Run this to make all sections work without props

echo "✅ Done! All sections now have optional data props with static defaults"
echo "💡 Sections will work without any props passed to them"
